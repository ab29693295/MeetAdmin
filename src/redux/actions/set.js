export const setAllRoles = (allRoles) =>
    ({
        type    : 'SET_ALL_ROLES',
        payload : { allRoles }
    });
export const setAllProjects = (allProjects) =>
    ({
        type    : 'SET_ALL_PROJECTS',
        payload : { allProjects }
    });
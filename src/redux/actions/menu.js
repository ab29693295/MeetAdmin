export const setSiderMenu = (siderList) =>
    ({
        type    : 'SET_SIDER_MENU',
        payload : { siderList }
    });
export const setAllMenus = (allMenus) =>
    ({
        type    : 'SET_ALL_MENUS',
        payload : { allMenus }
    });
